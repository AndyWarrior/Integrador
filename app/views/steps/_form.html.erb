<%= form_for(@step) do |f| %>
  <% if @step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@step.errors.count, "error") %> prohibited this step from being saved:</h2>

      <ul>
      <% @step.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Nombre" %><br>
    <%= f.text_field :name, :placeholder => "Nombre" %>
  </div>
  <div class="field">
    <%= f.label "Número" %><br>
    <%= f.number_field :numstep, :placeholder => "123" %>
  </div>
  <div class="field">
    <%= f.label "Fase" %><br>
    <%= select("step", "phase_id", Phase.all.collect {|p| [ p.name, p.id ] }) %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar Paso", :class => "btn btn-lg btn-primary cemex-blue btn-otros btn-block" %>
  </div>
<% end %>
