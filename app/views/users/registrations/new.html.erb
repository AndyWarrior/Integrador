
<div class="container  ">
  
  <div class= "col-sm-offset-4 col-sm-4">
<%= image_tag "logo.png" , class: "centrado logo"%>

<h2 class="centrado">Registrarse</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    
    <%= f.email_field :email, autofocus: true, :placeholder => "Usuario",:class => "form-signin" %>
  </div>

  <div class="field">
    
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> caracteres minimo)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "off", :placeholder => "Contraseña",:class => "form-signin" %>
  </div>

  <div class="field">
    
    <%= f.password_field :password_confirmation, autocomplete: "off" , :placeholder => "Contraseña",:class => "form-signin"%>
  </div>

  <div class="field">
    <%= f.label "Programa al que pertenezco" %><br>
    <%= f.collection_select :program, Program.all, :id, :name, :class => "form-signin"%>
  </div>

  <div class="field">
    <%= f.label "Nombre del proyecto" %><br>
    <%= f.collection_select :project, Project.all, :id, :name, :class => "form-signin"%>
  </div>

  <div class="actions">
    <%= f.submit "Registrarme", :class => "btn btn-lg btn-primary cemex-red btn-entrada btn-block" %>
  </div>
<% end %>

<%= render "users/shared/links" %>
</div>
</div>
